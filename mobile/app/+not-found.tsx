import { Redirect } from 'expo-router';

/**
 * Запасной маршрут для всего, что не разобралось.
 *
 * Зачем он нужен. Нажатие на уведомление плеера открывает приложение
 * системным интентом, и на части прошивок (HyperOS в их числе) при этом
 * приходит путь, которого в схеме роутинга нет. Без этого файла
 * expo-router показывает экран ошибки «route not found» — вместо
 * музыки пользователь видит поломку.
 *
 * Сюда же попадают устаревшие ссылки после переименования экранов
 * и любые опечатки в deep link со схемой ytmusic://.
 *
 * Молча уводим на главную: неизвестный адрес — не повод показывать
 * ошибку, приложение должно просто открыться.
 */
export default function NotFound() {
  return <Redirect href="/" />;
}
